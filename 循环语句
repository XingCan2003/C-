#include<stdio.h>
int main()
{
   int line = 0;
   printf("上大学\n");
   whlie(line<10)
   {
      printf("学习一次; %d\n", line);
      line++;
   }
   if(line>=10)
      printf("好成绩\n",linr);
   return 0;
}
