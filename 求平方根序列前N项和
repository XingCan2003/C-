#include<stdio.h>
#include<math.h>
int main()
{
    int i = 0,x;
    double sum = 0.0;
    scanf("%d", &x);
    for (i = 1; i <= x; i++)
    {
        sum += sqrt(i);
    }
    printf("sum = %0.2f", sum);
    return 0;
}
