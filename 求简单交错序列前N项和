#include<stdio.h>
int main()
{
    int i = 0;
    int n = 0;
    int flay = 1;
    double sum = 0.0;
    scanf("%d", &n);
    for (i = 1; i <= n; i ++)
    {
        sum += flay * 1.0 / (3*i - 2);
        flay = -flay;
    }
    printf("sum = %0.3lf", sum);
    return 0;
}
