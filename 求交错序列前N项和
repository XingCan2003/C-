#include<stdio.h>
int main()
{
    int i = 0;
    int n = 0;
    int flay = 1;
    double sum = 0.0;
    scanf("%d", &n);
    for (i = 1; i <= n; i++)
    {
        sum += flay * (i*1.0) / (2 * i - 1);
        flay = -flay;
    }
    printf("%0.3f", sum);
    return 0;
}
