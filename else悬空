#include <stdio.h>//else悬空

int main()
{
  int a = 0;
  int b = 2;
  if(a == 0)
   {
    if(b == 2)
      printf("hehe\n");
    }
  else//else需要加{}，不然不知道和哪个if匹配，
      printf("haha\n");//和第一个匹配，就给第二个加{}
  return 0;    //想和第二个匹配，把else和第二个if框在一起
}
